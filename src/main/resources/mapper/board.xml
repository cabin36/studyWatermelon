<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <insert id="saveFile" parameterType="boardFile">
        insert into board_file_table(originalFileName, storedFileName, boardId)
        values (#{originalFileName}, #{storedFileName}, #{boardId})
    </insert>

    <select id="findFile" parameterType="Long" resultType="boardFile">
        select * from board_file_table where boardId = #{id}
    </select>

    <insert id="insertBoard" parameterType="board">
        insert into boardWatermelon (user, content, title, fileAttached) values (#{user}, #{content}, #{title}, #{fileAttached})
    </insert>

   <select id="findAll" resultType="board">
        select id, boardTitle, user from boardWatermelon;
   </select>

    <select id="findById" parameterType="Long" resultType="board">
        select id, title, user, content from boardtest where id=#{id}
    </select>
</mapper>