<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <insert id="saveFile" parameterType="boardFile">
        insert into wmboard_file(originalFileName, storedFileName, boId)
        values (#{originalFileName}, #{storedFileName}, #{boId})
    </insert>

    <select id="findFile" parameterType="Long" resultType="boardFile">
        select * from wmboard_file where boId = #{id}
    </select>

    <insert id="insertBoard" parameterType="board">
        insert into wmboard (boWriter, boContent, boTitle, fileAttached) values (#{boWriter}, #{boContent}, #{boTitle}, #{fileAttached})
    </insert>

   <select id="findAll" resultType="board">
        select id, boWriter, user from wmboard;
   </select>

    <select id="findById" parameterType="Long" resultType="board">
        select id, boTitle, boWriter, boContent from wmboard where id=#{id}
    </select>

    <update id="update" parameterType="board">
        update wmboard
        set boTitle = #{boTitle}, boContent=#{boContent}
        where id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        delete from wmboard
        where id=#{id}
    </delete>
</mapper>